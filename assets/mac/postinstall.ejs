#!/bin/bash

set -euo pipefail

# execute application once to setup pointers to native messaging host application
$DSTROOT/<%= config.id %>.app/Contents/MacOS/<%= config.id %> install > /tmp/vdh.logs 2>&1
