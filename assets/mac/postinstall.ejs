#!/bin/bash

# Register the application into the different browser
# configuration files.

set -euo pipefail

bin=$DSTROOT/<%= config.id %>.app/Contents/MacOS/<%= binaryName %>

$bin install --system
$bin install --user
